"""
D0PA1 canonical log schema package (Part B, Gate 0). See
schema/canonical_log_v1.json (the schema document) and
schema/canonical_log_writer.py (the single writer/validator this
repository routes canonical-format logging through).
"""
